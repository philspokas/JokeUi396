name: Deployment

on:
  push:
    branches:
      - main
      - dev*
  workflow_dispatch:

env:
    deployment-value: "test deploymnet value"

jobs:

  deploy-dev:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - run: |
          printenv
          echo "Secret:"  
          echo '${{ secrets.ENVIRONMENTSECRET }}'
          echo "Variable:"  
          echo 'env var ${{ var.deployment-value }}'
        name: deploy
        # ...deployment-specific steps

  deploy-test:
    runs-on: ubuntu-latest
    environment: test
    needs: deploy-dev 
    steps:
      - run: |
          echo ${{ secrets.ENVIRONMENTSECRET }}
          echo ${{ vars.DEPLOYMENT }}
        name: deploy
        # ...deployment-specific steps

  deploy-prod:
    runs-on: ubuntu-latest
    environment: prod
    needs: deploy-test
    steps:
      - run: |
          echo ${{ secrets.ENVIRONMENTSECRET }}
          echo ${{ vars.DEPLOYMENT }}
        name: deploy
        # ...deployment-specific steps

